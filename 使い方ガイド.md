# PubMed → Google Doc 自動化ガイド

## 全体の流れ

```
① アイコンをダブルクリック（月1回、感染症だけ週1回）
        ↓
② Markdownファイルとテーマ別HTMLが自動生成される
        ↓
③ ChatGPT Thinking貼付用txtも自動生成できる
        ↓
④ ChatGPT Projectへ貼り付けて重要論文10本を厳選・要約
        ↓
⑤ Google DocをNotebookLMに登録 → 音声解説を聞く
```

---

## ① スクリプトの実行方法

### いちばん簡単な方法：ダブルクリック

通常は、このファイルだけを使えば十分です。

- `PubMed_Abstract作成.app`

ダブルクリックするとメニューが開き、以下を選べます。

- Abstract抽出のみ
- AIで10本を選定
- ChatGPT Thinking貼付用txtを作成してコピー
- 月次テーマすべて
- 週次の小児感染症
- 全テーマすべて
- 個別テーマ

完了すると、抽出のみの場合は `google_docs`、APIでAI選定の場合は `ai_selected`、ChatGPT貼付用の場合は `chatgpt_paste` フォルダが開きます。

旧ショートカットや参考資料は `archive/` に移しました。通常は触る必要はありません。

初回だけMacの警告が出る場合があります。その場合は、右クリックして「開く」を選んでください。

### APIなしでChatGPT Thinkingを使う方法

ChatGPT Plus / ProのProject Instructionsに、選定基準と出力形式を保存しておきます。

`PubMed_Abstract作成.app` で以下を選びます。

1. `ChatGPT Thinking貼付用txtを作成してコピー`
2. 実行したいテーマ

完了すると以下が作られます。

- `chatgpt_paste/YYYYMM/テーマ名_ChatGPT貼付用.txt`

同時に内容がMacのクリップボードへコピーされます。ChatGPT Projectを開いて、そのまま貼り付ければ使えます。

貼付用txtの中身:

- 領域
- 対象期間
- Title
- PMID
- Journal
- Abstract

HTMLタグや長いプロンプトは入れないため、ChatGPT Thinkingに貼り付けやすく、PMIDの誤認も起きにくくなります。

### AIで10本を選定する方法

OpenAI APIキーを設定すると、検索後にAIが重要論文10本を選び、日本語評価と英語abstract集を1つのHTMLにまとめます。

使うファイル:

- `PubMed_Abstract作成.app`

出力先:

- `ai_selected/YYYYMM/テーマ名.html`

HTMLの中身:

- 選定10本の一覧表
- スコア
- 役立つかどうか
- 診療変更の必要性
- 日本語要約
- 選定10本の英語abstract全文
- 他テーマとPMIDが重複した場合の表示
- 候補論文スコア一覧

除外論文について:

- NotebookLM用HTMLには詳しい除外理由や落選論文のabstract全文は入れません
- ただし、PMID・タイトル・総合スコア・役立つか・短いメモの一覧は入れます
- 全候補のスコアは同じフォルダの `.json` に記録されます
- 通常はHTMLだけ見れば十分です

重複PMIDの扱い:

- 同じ月の別テーマで既に選ばれたPMIDは `selected_pmids_history.json` に記録されます
- 重複論文がそのテーマでも重要な場合は残します
- その代わり、可能な範囲で追加枠を入れ、11本目以降として別論文も読めるようにします

APIキーの設定:

1. `.env.example` を複製して、名前を `.env` に変更
2. `.env` を開く
3. `OPENAI_API_KEY=sk-...` の形でAPIキーを貼り付ける

`.env` には秘密のAPIキーが入るため、他人に共有しないでください。

Google Docsへの保存:

- 現時点では `ai_selected/YYYYMM/テーマ名.html` を作成します
- このHTMLをGoogle DriveにアップロードするとGoogle Docsに変換できます
- Codexアプリ内ではGoogle Drive連携を使って変換アップロードできます
- ダブルクリックだけでGoogle Docs保存まで完結させるには、Google Drive APIの認証設定が追加で必要です

### ChatGPTの回答をGoogle Docs用に保存する方法

ChatGPT Thinkingが出した最終回答をGoogle Docsへ持っていく場合は、以下の手順が簡単です。

1. ChatGPTの回答をコピー
2. `ChatGPT出力をGoogleDocs用に保存.app` をダブルクリック
3. テーマを番号で選択
4. `chatgpt_outputs/YYYYMM/` に `.html` と `.txt` が保存される
5. `.html` をGoogle DriveへアップロードしてGoogle Docs化

ファイル名は自動生成されます。

- 月次テーマ: `2026-05_小児心身症_abstract10本`
- 週次小児感染症: `2026-05_w1_小児感染症_abstract10本`

この方法ではGoogle APIキーは不要です。Google Docsへの完全自動保存ではありませんが、コピー後の整形とファイル保存までは自動化できます。

---

## フォルダ構成

普段使うもの:

- `PubMed_Abstract作成.app`
- `ChatGPT出力をGoogleDocs用に保存.app`
- `使い方ガイド.md`

主な出力先:

- `chatgpt_paste/`: ChatGPT Projectへ貼り付けるためのtxt
- `chatgpt_outputs/`: ChatGPTが出した最終回答をGoogle Docs用に保存したHTML/TXT
- `google_docs/`: PubMed抽出HTML
- `ai_selected/`: OpenAI APIでAI選定した場合のHTML/JSON

アーカイブ:

- `archive/launchers/`: `.app` が内部で呼び出す `.command`
- `archive/old_shortcuts/`: 以前作成した個別ショートカット
- `archive/reference/`: 参考資料、スクリーンショット、旧プロンプト
- `archive/generated_outputs/`: 以前生成したMarkdown出力

---

### ターミナルで実行する方法

### ターミナルを開く

Macの「Launchpad」または「Spotlight（⌘ + スペース）」で
**「ターミナル」** と検索して開きます。

### 毎月の実行（月次テーマ）

```bash
python3 ~/Documents/GitHub/pubmed_abstract/pubmed_fetch.py --frequency monthly
```

完了すると以下が同フォルダに保存されます。

- `abstracts_YYYYMM_monthly.md`
- `google_docs/YYYYMM/テーマ名.html`

### 小児感染症だけ週1回実行

```bash
python3 ~/Documents/GitHub/pubmed_abstract/pubmed_fetch.py --frequency weekly
```

この場合は `abstracts_YYYYMM_weekly.md` が作成されます。

### 1テーマだけ実行

```bash
python3 ~/Documents/GitHub/pubmed_abstract/pubmed_fetch.py --topic asthma
python3 ~/Documents/GitHub/pubmed_abstract/pubmed_fetch.py --topic constipation
python3 ~/Documents/GitHub/pubmed_abstract/pubmed_fetch.py --topic 小児感染症
```

英語名・日本語名のどちらでも指定できます。
この場合は `abstracts_YYYYMM_asthma.md` のようにテーマ名入りのファイルが作成されるため、月次ファイルを上書きしません。

---

## ② Google Docsへのアップロード

`google_docs/YYYYMM/` フォルダ内に、テーマ別のHTMLファイルが作成されます。

Google Driveにアップロードすると、Google Docとして開けます。
NotebookLMには、そのGoogle Docをそのまま登録できます。

HTMLの中には以下が入っています。

- ChatGPT / Gemini / Claude へ依頼するためのプロンプト
- PubMedから取得した全Abstract
- PMIDとPubMedリンク

重要論文10本への厳選と日本語要約まで済ませたGoogle Docを作りたい場合は、
HTML冒頭のプロンプトをChatGPT / Gemini / Claudeに貼り付けてください。

---

## ③ ファイル名と構成

**ファイル名の形式:**
```
YYYY-MM_テーマ名_PubMed抽出
```

**例:**
- `2026-05_気管支喘息_PubMed抽出`
- `2026-05_便秘_PubMed抽出`
- `2026-05_小児感染症_PubMed抽出`

**Google Docの構成:**

```
【ChatGPT / Gemini / Claude への依頼文】
重要論文10本の厳選・要約用プロンプト

【PubMed Abstract一覧】
1. 論文タイトル
PMID
Journal
Abstract

---
2.
...
```

---

## ④ NotebookLMへの登録（手動・2分）

1. [notebooklm.google.com](https://notebooklm.google.com) を開く
2. 「新しいノートブック」を作成（またはテーマ別の既存ノートブックに追加）
3. 「ソースを追加」→「Google ドライブ」→ Claudeが作ったDocを選択
4. 「音声の概要」をクリック → 生成を待つ

---

## テーマ一覧とファイル名ラベル

| 検索名 | Googleドキュメント名（テーマ部分） |
|--------|----------------------------------|
| primary_care_review | プライマリケアレビュー |
| Pediatric_Primary_Care_High_Impact | 小児プライマリケア |
| ped_sleep_update | 小児睡眠 |
| ped_psycho_update | 小児心身症 |
| ped_trauma_update | 小児外傷 |
| ped_vaccine_update | 小児ワクチン |
| ped_nephrology_update | 小児腎臓 |
| ped_constipation_update | 便秘 |
| ped_enuresis_update | 夜尿症 |
| ped_infection_1w_update | 小児感染症 |
| ped_food_update | 食物アレルギー |
| ped_atopic_update | アトピー性皮膚炎 |
| peds_asthma_update | 気管支喘息 |
| ped_development_update | 発達障害 |

---

## トラブルシューティング

| 症状 | 対処 |
|------|------|
| `python3: command not found` | `python` で試す。それも無ければ [python.org](https://python.org) からインストール |
| 特定テーマだけ0件 | そのテーマのクエリが古い可能性。PubMedで確認して `pubmed_fetch.py` の該当クエリを修正 |
| ファイルが見当たらない | `~/Documents/GitHub/pubmed_abstract/` フォルダをFinderで確認 |
| 感染症の件数が少ない | 週次実行では直近7日で絞っています。月単位で見たい場合は `pubmed_fetch.py` の `ped_infection_1w_update` の `reldate` を30に変更 |

---

*更新日: 2026年5月*
